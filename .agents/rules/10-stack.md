# Desktop Engine Stack Conventions
- Tauri v2, Rust (`src-tauri`), Swift/Metal native wallpaper underlay, ESBuild web runtime.
- Rust standards: Zero warnings allowed in `src-tauri`. Always verify with `cargo check`.
